export interface PhoneResponse {
    grantType: string
    accessToken: string
    refreshToken: string
}

export interface SchoolAuthResponse {
    grantType: string
    accessToken: string
    refreshToken: string
}

export interface LoginResponse {
    grantType: string
    accessToken: string
    refreshToken: string
}
